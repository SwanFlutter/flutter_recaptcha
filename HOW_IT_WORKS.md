# نحوه کار Rotation CAPTCHA

## مفهوم اصلی

Rotation CAPTCHA بر اساس این ایده کار می‌کند که انسان‌ها می‌توانند به راحتی تشخیص دهند که یک تصویر چرخیده است یا خیر، اما برای ربات‌ها این کار دشوار است.

## مراحل کار

### 1. نمایش اولیه

```
┌─────────────────────┐
│   تصویر کامل        │  ← قسمت بیرونی (ثابت)
│   ┌───────────┐     │
│   │ تصویر     │     │  ← قسمت داخلی (چرخیده)
│   │ چرخیده   │     │
│   └───────────┘     │
└─────────────────────┘
```

- **قسمت بیرونی:** تصویر کامل در حالت صحیح (0 درجه)
- **قسمت داخلی:** همان تصویر اما چرخیده شده (مثلاً 120 درجه)

### 2. تولید چالش

```dart
void _generateChallenge() {
  // تولید زاویه تصادفی بین 30 تا 330 درجه
  _initialRotation = 30 + Random().nextDouble() * 300;
  _currentRotation = _initialRotation;
}
```

چرا بین 30 تا 330؟
- زاویه‌های خیلی کوچک (< 30) خیلی ساده هستند
- زاویه‌های نزدیک به 360 درجه با 0 درجه اشتباه گرفته می‌شوند

### 3. تعامل کاربر

```dart
void _onSliderChanged(double value) {
  // value: 0.0 تا 1.0
  // چرخش از زاویه اولیه به سمت 0
  _currentRotation = _initialRotation * (1 - value);
}
```

مثال:
- زاویه اولیه: 120 درجه
- اسلایدر در 0%: چرخش = 120 درجه
- اسلایدر در 50%: چرخش = 60 درجه
- اسلایدر در 100%: چرخش = 0 درجه ✅

### 4. تایید

```dart
Future<void> _verify() async {
  final difference = _currentRotation.abs();
  final isSuccess = difference <= tolerance; // مثلاً 15 درجه
  
  if (isSuccess) {
    // موفق! ✅
    onSuccess?.call();
  } else {
    // ناموفق! ❌
    // تولید چالش جدید
    _generateChallenge();
  }
}
```

## مثال عملی

فرض کنید تصویر یک سگ داریم:

### حالت 1: شروع
```
زاویه اولیه: 90 درجه
┌─────────────┐
│    🐕       │  ← سگ عمودی (صحیح)
│  ┌─────┐   │
│  │ 🐕  │   │  ← سگ افقی (چرخیده 90 درجه)
│  └─────┘   │
└─────────────┘
```

### حالت 2: کاربر اسلایدر را 50% می‌کشد
```
زاویه فعلی: 45 درجه
┌─────────────┐
│    🐕       │  ← سگ عمودی (صحیح)
│  ┌─────┐   │
│  │ 🐕  │   │  ← سگ 45 درجه چرخیده
│  └─────┘   │
└─────────────┘
```

### حالت 3: کاربر اسلایدر را 100% می‌کشد
```
زاویه فعلی: 0 درجه
┌─────────────┐
│    🐕       │  ← سگ عمودی (صحیح)
│  ┌─────┐   │
│  │ 🐕  │   │  ← سگ عمودی (صحیح) ✅
│  └─────┘   │
└─────────────┘
```

## چرا این روش امن است؟

### 1. تصادفی بودن
هر بار زاویه متفاوتی تولید می‌شود، پس ربات نمی‌تواند یک الگوی ثابت یاد بگیرد.

### 2. نیاز به تشخیص بصری
ربات باید بتواند:
- تصویر را تشخیص دهد
- بفهمد که چه چیزی در تصویر است
- تشخیص دهد که تصویر چرخیده است
- زاویه چرخش را محاسبه کند

### 3. تعامل انسانی
کاربر باید اسلایدر را به صورت دستی بکشد، که برای ربات‌های ساده دشوار است.

## پارامترهای قابل تنظیم

### tolerance (دقت)
```dart
RotationCaptchaWidget(
  tolerance: 15.0, // پیش‌فرض
)
```

- **کم (5-10 درجه):** سخت‌تر، نیاز به دقت بیشتر
- **متوسط (15-20 درجه):** متعادل
- **زیاد (25-30 درجه):** آسان‌تر

### animationDuration (سرعت انیمیشن)
```dart
RotationCaptchaWidget(
  animationDuration: Duration(milliseconds: 300), // پیش‌فرض
)
```

## نکات پیاده‌سازی

### 1. استفاده از یک تصویر
```dart
// قسمت بیرونی
Image.asset(imagePath, fit: BoxFit.cover)

// قسمت داخلی (همان تصویر اما چرخیده)
Transform.rotate(
  angle: _currentRotation * pi / 180,
  child: Image.asset(imagePath, fit: BoxFit.cover),
)
```

### 2. ClipOval برای دایره
```dart
ClipOval(
  child: Image.asset(imagePath),
)
```

### 3. Border برای نمایش وضعیت
```dart
Border.all(
  color: _verificationResult == null
      ? Colors.white      // در حال تلاش
      : _verificationResult!
          ? Colors.green  // موفق ✅
          : Colors.red,   // ناموفق ❌
  width: 4,
)
```

## مقایسه با روش‌های دیگر

| روش | مزایا | معایب |
|-----|-------|-------|
| **Rotation CAPTCHA** | جذاب، کاربرپسند، امن | نیاز به تصویر مناسب |
| **reCAPTCHA v2** | بسیار امن | تجربه کاربری ضعیف |
| **reCAPTCHA v3** | بدون تعامل | ممکن است کاربران واقعی را رد کند |
| **Checkbox CAPTCHA** | ساده | آسان برای ربات‌ها |

## نتیجه‌گیری

Rotation CAPTCHA یک روش متعادل بین امنیت و تجربه کاربری است که:
- برای کاربران جذاب و سرگرم‌کننده است
- برای ربات‌های ساده دشوار است
- قابل تنظیم و سفارشی‌سازی است
- به راحتی قابل پیاده‌سازی است
